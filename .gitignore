# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js build output
/.next
/out

# Production
/build

# Misc
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (keep them secret, don’t commit)
.env*
/.env.local
/.env.development
/.env.test
/.env.production

# Editor stuff
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp

# Optional: ignore lockfiles if using a single package manager
# If you’re using npm, keep package-lock.json and remove the next 2 lines
# yarn.lock
# pnpm-lock.yaml

# Optional: ignore lockfiles
package-lock.json
yarn.lock
pnpm-lock.yaml

# OS
Thumbs.db
